# 数据库目录说明

本目录用于存放课程基座的 SQLite 初步结构、种子数据和后续迁移脚本。

当前阶段先提供：

- `schema.sql`：统一平台的第一版表结构草案
- `seed.sql`：教学演示用的脱敏初始数据

后续规则：

- 学生不得直接提交本机生成的 `.db`、`.sqlite` 或 `.sqlite3` 文件
- 各组如需新增字段或表，必须先说明其与公共基础层和 `form_index` 的关系
- 数据表命名、字段命名和索引策略应保持统一
